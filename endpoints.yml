#endpoints.yml

action_endpoint:
  url: "http://localhost:5055/webhook"

# Persistir conversaciones para que los mensajes anteriores se restablezcan cuando un
# user vuelve a iniciar sesión.  Almacenar el tracker en un archivo
# SQLite garantiza que el historial se mantenga incluso si el servidor
# Rasa se reinicia.
tracker_store:
  type: SQL
  dialect: "sqlite"
  db: "tracker.db"